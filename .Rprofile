.First <- function() {
  options(
    repos = c(CRAN = "http://cran.rstudio.com/"),
    browserNLdisabled = TRUE,
    deparse.max.lines = 2,
    devtools.name = "Dan Knoepfle",
    devtools.desc.author = "Dan Knoepfle <knoepfle@gmail.com> [aut, cre]",
    devtools.desc.license = "MIT")
}

if (interactive()) {
  suppressMessages(require(devtools))
}
